function over(element, ) {
    element.img.src = "";
    element.style.backgroundColor = "white"
}

function out(element) {
    element.innerText = "sample project 1";
    element.style.backgroundColor = "none"

}